{
  "name": "supplement-facts",
  "version": "0.0.1",
  "private": true,

  "scripts": {
    "prestart": "npm install",
    "start": "http-server -p 3000"
  },

  "devDependencies": {
    "http-server": "^0.9.0"
  }
}
